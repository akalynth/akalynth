Before making a commit, enforce atomic discipline:

- One responsibility only
- No formatting-only noise mixed with logic
- Verification steps relevant to the commit pass
- Commit message uses: <area>: <what>

If changes are too broad, propose a split into multiple commits and list them in order.
