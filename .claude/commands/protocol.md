Read packages/shared/protocol.ts and ensure docs/PROTOCOL.md matches it.

- List all message types
- Include request/response JSON examples
- Include error codes
- If docs diverge, update docs to match code (code is source of truth)
