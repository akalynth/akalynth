# @treasury-check

Purpose: Verify gold/item counts are consistent with receipts / projections.

Run:
- npm run verify:treasury (or existing treasury verification script)

Rules:
- Do not "fix" balances; only report mismatches + evidence.
