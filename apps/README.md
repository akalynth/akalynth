# Apps
Deployable application entrypoints (server, clients, diagnostics).
Each app owns runtime config, launch scripts, and packaging.
Legacy app code may still live in `server/` or `client/` until migrated.
